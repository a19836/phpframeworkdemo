<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sql_mapping>
	<insert id="insert_activity">
		insert into mu_activity (name, created_date, modified_date) values ('#name#', '#created_date#', '#modified_date#')
	</insert>
	<insert id="insert_activity_with_ai_pk" hard_coded_ai_pk="1">
		insert into mu_activity (activity_id, name, created_date, modified_date) values (#activity_id#, '#name#', '#created_date#', '#modified_date#')
	</insert>
	
	<update id="update_activity">
		update mu_activity set name='#name#', modified_date='#modified_date#' where activity_id=#activity_id#
	</update>
	
	<delete id="delete_activity">
		delete from mu_activity where activity_id=#activity_id#
	</delete>
	
	<select id="get_activity">
		select * from mu_activity where activity_id=#activity_id#
	</select>
	
	<select id="get_all_activities">
		select * from mu_activity
	</select>
	
	<select id="count_all_activities">
		select count(activity_id) total from mu_activity
	</select>
	
	<select id="get_activities_by_conditions">
		select * from mu_activity where #conditions#
	</select>
	
	<select id="count_activities_by_conditions">
		select count(activity_id) total from mu_activity where #conditions#
	</select>
</sql_mapping>
