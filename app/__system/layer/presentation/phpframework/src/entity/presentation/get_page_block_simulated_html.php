<?php
/*
 * Copyright (c) 2025 Bloxtor (http://bloxtor.com) and Joao Pinto (http://jplpinto.com)
 * 
 * Multi-licensed: BSD 3-Clause | Apache 2.0 | GNU LGPL v3 | HLNC License (http://bloxtor.com/LICENSE_HLNC.md)
 * Choose one license that best fits your needs.
 */
 include_once $EVC->getUtilPath("CMSPresentationLayerHandler"); $UserAuthenticationHandler->checkPresentationFileAuthentication($entity_path, "access"); $bean_name = isset($_GET["bean_name"]) ? $_GET["bean_name"] : null; $bean_file_name = isset($_GET["bean_file_name"]) ? $_GET["bean_file_name"] : null; $path = isset($_GET["path"]) ? $_GET["path"] : null; $project = isset($_GET["project"]) ? $_GET["project"] : null; $block = isset($_GET["block"]) ? $_GET["block"] : null; $data = json_decode( htmlspecialchars_decode( file_get_contents("php://input"), ENT_NOQUOTES), true); $page_region_block_type = isset($data["page_region_block_type"]) ? $data["page_region_block_type"] : null; $page_region_block_params = isset($data["page_region_block_params"]) ? $data["page_region_block_params"] : null; $page_region_block_join_points = isset($data["page_region_block_join_points"]) ? $data["page_region_block_join_points"] : null; $path = str_replace("../", "", $path); $html = $editable_settings = $block_code_id = $block_code_time = null; if ($path && $project && $block) { $WorkFlowBeansFileHandler = new WorkFlowBeansFileHandler($user_beans_folder_path . $bean_file_name, $user_global_variables_file_path); $PEVC = $WorkFlowBeansFileHandler->getEVCBeanObject($bean_name, $path); if ($PEVC) { $PHPVariablesFileHandler = new PHPVariablesFileHandler(array($user_global_variables_file_path, $PEVC->getConfigPath("pre_init_config"))); $PHPVariablesFileHandler->startUserGlobalVariables(); $P = $PEVC->getPresentationLayer(); $page_include_files = getPageIncludeFiles($PEVC, isset($data["template_includes"]) ? $data["template_includes"] : null); $P->setSelectedPresentationId($project); $is_block = $page_region_block_type != "view"; $is_view = $page_region_block_type == "view"; $block_path = $is_view ? $PEVC->getViewPath($block) : $PEVC->getBlockPath($block); if (file_exists($block_path)) { $module_id = $block_params = null; if ($is_block) { $block_params = CMSFileHandler::getFileCreateBlockParams($block_path, false, 1, 1); $module_id = isset($block_params[0]["module_type"]) && $block_params[0]["module_type"] == "string" ? $block_params[0]["module"] : ""; $block_code_id = md5(file_get_contents($block_path)); $block_code_time = filemtime($block_path); } if ($module_id) { $CMSModuleSimulatorHandler = $EVC->getCMSLayer()->getCMSModuleLayer()->getModuleSimulatorObj($module_id); if ($CMSModuleSimulatorHandler) { $CMSModuleLayer = $PEVC->getCMSLayer()->getCMSModuleLayer(); $CMSModuleHandler = $CMSModuleLayer->getModuleObj($module_id); if ($CMSModuleHandler) { $cms_settings = array( "module_id" => $module_id, "block_id" => $block, ); $CMSModuleHandler->setCMSSettings($cms_settings); $CMSModuleSimulatorHandler->setCMSModuleHandler($CMSModuleHandler); $block_settings = getProjectBlockSettings($PEVC, $project, $block_params, $page_include_files, $page_region_block_params); if ($page_region_block_join_points) { foreach ($page_region_block_join_points as $join_point_name => $join_point) { foreach ($join_point as $idx => $join_point_settings) if (is_array($join_point_settings)) { $join_point_settings = getProjectJoinPointSettings($PEVC, $project, $page_include_files, $join_point_settings); $PEVC->getCMSLayer()->getCMSJoinPointLayer()->addBlockJoinPoint($block, $join_point_name, $join_point_settings); } } } $html = $CMSModuleSimulatorHandler->simulate($block_settings, $editable_settings); } } } else { $html = getProjectBlockHtml($PEVC, $project, $block_path, $page_include_files); } } $PHPVariablesFileHandler->endUserGlobalVariables(); } } function getPageIncludeFiles($EVC, $includes) { $page_include_files = array(); if (is_array($includes)) { foreach ($includes as $include) if (!empty($include["path"])) { $code = "\$include_path = " . $include["path"] . ";"; eval($code); if ($include_path && file_exists($include_path)) $page_include_files[] = $include_path; } } return $page_include_files; } function getProjectJoinPointSettings($EVC, $selected_project_id, $page_include_files, $join_point_settings) { $before_defined_vars = get_defined_vars(); include $EVC->getConfigPath("config", $selected_project_id); include_once $EVC->getUtilPath("include_text_translator_handler", $EVC->getCommonProjectName()); if (is_array($page_include_files)) { foreach ($page_include_files as $include) @include_once $include; } $after_defined_vars = get_defined_vars(); $external_vars = array_diff_key($after_defined_vars, $before_defined_vars); $external_vars["EVC"] = $EVC; unset($external_vars["before_defined_vars"]); $code = "<?php return " . CMSPresentationLayerHandler::getJoinPointPropertiesCode($join_point_settings) . '; ?>'; PHPScriptHandler::parseContent($code, $external_vars, $return_values); $join_point_settings = isset($return_values[0]) ? $return_values[0] : null; return $join_point_settings; } function getProjectBlockSettings($EVC, $selected_project_id, $block_params, $page_include_files, $page_region_block_params) { $before_defined_vars = get_defined_vars(); include $EVC->getConfigPath("config", $selected_project_id); include_once $EVC->getUtilPath("include_text_translator_handler", $EVC->getCommonProjectName()); if (is_array($page_include_files)) { foreach ($page_include_files as $include) @include_once $include; } $after_defined_vars = get_defined_vars(); $external_vars = array_diff_key($after_defined_vars, $before_defined_vars); $external_vars["EVC"] = $EVC; unset($external_vars["before_defined_vars"]); $code = "<?php \n"; if ($page_region_block_params) { $arr_code = ""; foreach($page_region_block_params as $k => $v) $arr_code .= ($arr_code ? ", " : "") . "$k => $v"; $code .= '$block_local_variables = array(' . $arr_code . ");\n"; } $code .= 'return '; if (isset($block_params[0]["block_settings"]) && is_array($block_params[0]["block_settings"])) $code .= WorkFlowTask::getArrayString($block_params[0]["block_settings"]); else $code .= isset($block_params[0]["block_settings"]) && strlen($block_params[0]["block_settings"]) ? PHPUICodeExpressionHandler::getArgumentCode($block_params[0]["block_settings"], $block_params[0]["block_settings_type"]) : '""'; $code .= '; ?>'; PHPScriptHandler::parseContent($code, $external_vars, $return_values); $block_settings = isset($return_values[0]) ? $return_values[0] : null; return $block_settings; } function getProjectBlockHtml($EVC, $selected_project_id, $block_path, $page_include_files) { include $EVC->getConfigPath("config", $selected_project_id); include_once $EVC->getUtilPath("include_text_translator_handler", $EVC->getCommonProjectName()); if (is_array($page_include_files)) { foreach ($page_include_files as $include) @include_once $include; } ob_start(null, 0); include $block_path; $html = ob_get_contents(); ob_end_clean(); return $html; } ?>
