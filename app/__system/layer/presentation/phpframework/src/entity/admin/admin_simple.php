<?php
/*
 * Copyright (c) 2024 Bloxtor - http://bloxtor.com
 * 
 * Please note that this code belongs to the Bloxtor framework and must comply with the Bloxtor license.
 * If you do not accept these provisions, or if the Bloxtor License is not present or cannot be found, you are not entitled to use this code and must stop and delete it immediately.
 */
include_once $EVC->getUtilPath("CMSPresentationLayerHandler"); $UserAuthenticationHandler->checkPresentationFileAuthentication($entity_path, "access"); $choose_available_project_url = "{$project_url_prefix}admin/choose_available_project?redirect_path=admin"; $default_page = $_GET["default_page"]; $layers_beans = AdminMenuHandler::getLayers($user_global_variables_file_path); if ($layers_beans && $layers_beans["presentation_layers"]) { if (array_key_exists("filter_by_layout", $_GET) && $_GET["filter_by_layout"]) { foreach ($layers_beans["presentation_layers"] as $bn => $bfn) { $layer_bean_folder_name = WorkFlowBeansFileHandler::getLayerBeanFolderName($user_beans_folder_path . $bfn, $bn, $user_global_variables_file_path); if (strpos($_GET["filter_by_layout"], $layer_bean_folder_name . "/") === 0) { $bean_name = $bn; $bean_file_name = $bfn; $project = substr($_GET["filter_by_layout"], strlen($layer_bean_folder_name) + 1); CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_bean_name", $bean_name); CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_bean_file_name", $bean_file_name); CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_project", $project); } } } else { if ($default_page) CookieHandler::setCurrentDomainEternalRootSafeCookie("default_page", $default_page); else $default_page = $_COOKIE["default_page"]; if (!empty($_GET["bean_name"])) { $bean_name = $_GET["bean_name"]; CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_bean_name", $bean_name); } else if (!empty($_COOKIE["selected_bean_name"])) $bean_name = $_COOKIE["selected_bean_name"]; if (!empty($_GET["bean_file_name"])) { $bean_file_name = $_GET["bean_file_name"]; CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_bean_file_name", $bean_file_name); } else if (!empty($_COOKIE["selected_bean_file_name"])) $bean_file_name = $_COOKIE["selected_bean_file_name"]; if (!empty($_GET["project"])) { $project = $_GET["project"]; CookieHandler::setCurrentDomainEternalRootSafeCookie("selected_project", $project); } else if (!empty($_COOKIE["selected_project"])) $project = $_COOKIE["selected_project"]; } } if (!$bean_name || !$bean_file_name || !$project) { header("Location: $choose_available_project_url"); die("<script>document.location = '$choose_available_project_url';</script>"); } else if ($layers_beans["presentation_layers"][$bean_name] == $bean_file_name) { $layer_bean_folder_name = WorkFlowBeansFileHandler::getLayerBeanFolderName($user_beans_folder_path . $bean_file_name, $bean_name, $user_global_variables_file_path); $filter_by_layout = "$layer_bean_folder_name/" . preg_replace("/\/+$/", "", $project); $filter_by_layout_permission = UserAuthenticationHandler::$PERMISSION_BELONG_NAME; } $do_not_filter_by_layout = array( "bean_name" => $bean_name, "bean_file_name" => $bean_file_name, ); include $EVC->getUtilPath("admin_uis_layers_and_permissions"); $projects = null; if ($layers["presentation_layers"]) foreach ($layers["presentation_layers"] as $layer_name => $layer) if ($layer["properties"]["bean_name"] == $bean_name && $layer["properties"]["bean_file_name"] == $bean_file_name) { $projects = $presentation_projects[$layer_name]; break; } if (!$projects || !$projects[$project]) { header("Location: $choose_available_project_url"); echo "<script>document.location='$choose_available_project_url';</script>"; die(); } ?>
