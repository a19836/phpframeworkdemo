<?php
/*
 * Copyright (c) 2024 Bloxtor - http://bloxtor.com
 * 
 * Please note that this code belongs to the Bloxtor framework and must comply with the Bloxtor license.
 * If you do not accept these provisions, or if the Bloxtor License is not present or cannot be found, you are not entitled to use this code and must stop and delete it immediately.
 */
include_once get_lib("org.phpframework.util.text.TextSanitizer"); $UserAuthenticationHandler->checkPresentationFileAuthentication($entity_path, "access"); $popup = isset($_GET["popup"]) ? $_GET["popup"] : null; $last_file_created_time = isset($_GET["file_created_time"]) ? $_GET["file_created_time"] : null; $last_file_pointer = isset($_GET["file_pointer"]) ? $_GET["file_pointer"] : null; $number_of_lines = isset($_GET["number_of_lines"]) ? $_GET["number_of_lines"] : null; $ajax = isset($_GET["ajax"]) ? $_GET["ajax"] : null; $file_path = $GLOBALS["GlobalLogHandler"]->getFilePath(); if (file_exists($file_path)) { $file_created_time = filectime($file_path); $file_pointer = filesize($file_path); if ($last_file_pointer) { $output = $GLOBALS["GlobalLogHandler"]->tail(0, $last_file_pointer); } else { $number_of_lines = $number_of_lines > 0 ? $number_of_lines : 100; $output = $GLOBALS["GlobalLogHandler"]->tail($number_of_lines); } $output = TextSanitizer::convertBinaryCodeInTextToBase64($output); } if ($ajax) { $obj = array( "output" => isset($output) ? $output : null, "file_created_time" => isset($file_created_time) ? $file_created_time : null, "file_pointer" => isset($file_pointer) ? $file_pointer : null ); echo json_encode($obj); die(); } ?>
